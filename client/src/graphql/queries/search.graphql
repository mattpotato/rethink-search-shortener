query Search($query: String!, $first: Int, $after: Cursor) {
  search(query: $query, first: $first, after: $after) {
    nodes {
      firstName
      lastName
      jobTitle
      country
      country
      email
    }
    totalCount
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
